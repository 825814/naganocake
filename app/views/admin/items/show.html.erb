<p id="notice"></p>
<div class="container">

  <div class="row">
    <h5 class="bg-light text-dark">商品詳細</h5>
  </div>

  <div class="row">
    <div class="col-md-5">
      <%= image_tag @item.get_image(400,300) %>
    </div>

    <div class="col-md-7">
      <p>商品名<%= @item.name %></p>
      <p>商品説明<%= @item.introduction %></p>
      <p>ジャンル<%= @item.genre.name %></p>
      <p>税込価格<br>（税抜価格）<%= (@item.price * 1.1).round.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)円</p>
      <p>販売ステータス<% if @item.is_active == true %>
                         <!--<p class="text-success"><strong>販売中</strong></p>-->
                         <strong class="text-success">販売中</strong>
                       <% elsif @item.is_active == false %>
                         <!--<p class="text-secondary"><strong>販売停止中</strong></p>-->
                         <strong class="text-secondary">販売停止中</strong>
                       <% end %></p>

      <%= link_to "編集する", edit_admin_item_path, class: "btn btn-success btn-sm" %>

    </div>
  </div>
</div>

